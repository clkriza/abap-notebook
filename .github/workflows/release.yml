name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS — Universal binary (Apple Silicon + Intel)
          - platform: macos-latest
            args: '--target universal-apple-darwin'
            rust-targets: 'aarch64-apple-darwin,x86_64-apple-darwin'

          # Windows — NSIS (.exe) + MSI (.msi)
          - platform: windows-latest
            args: ''
            rust-targets: ''

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build and release (tauri-action)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'ABAP Notebook ${{ github.ref_name }}'
          releaseBody: |
            ## ABAP Notebook ${{ github.ref_name }}

            SAP / ABAP geliştiricileri için masaüstü dokümantasyon aracı.
            A desktop documentation tool for SAP / ABAP developers.

            ### 📥 İndirme / Download

            | Platform | Dosya / File | Açıklama / Description |
            |----------|-------|----------|
            | macOS | `.dmg` | Universal — Apple Silicon & Intel |
            | Windows | `.msi` | Windows Installer (önerilen / recommended) |
            | Windows | `.exe` | NSIS Setup Wizard |

            ### Kurulum / Installation

            **macOS:** `.dmg` dosyasını açıp uygulamayı `Applications` klasörüne sürükleyin.
            Open the `.dmg` and drag the app to `Applications`.
            > İlk açılışta Gatekeeper uyarısı çıkabilir: **Sistem Ayarları → Gizlilik & Güvenlik → Yine de Aç**
            > On first launch, Gatekeeper may warn you: **System Settings → Privacy & Security → Open Anyway**

            **Windows:** `.msi` veya `.exe` dosyasını çalıştırın.
            Run the `.msi` or `.exe` installer.

            ---
            Tüm veriler bilgisayarınızda yerel olarak saklanır. İnternet bağlantısı gerekmez.
            All data is stored locally on your machine. No internet connection required.
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
